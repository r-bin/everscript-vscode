{
  "name": "everscript",
  "displayName": "Everscript",
  "description": "Syntax highlighting, hover docs, and completions for Everscript (.evs) — the Secret of Evermore ROM scripting language",
  "version": "0.5.5",
  "publisher": "rbin",
  "main": "./extension.js",
  "engines": {
    "vscode": "^1.85.0"
  },
  "activationEvents": [
    "onLanguage:everscript",
    "onCommand:everscript.openMemoryRadar",
    "onCommand:everscript.openEmulator",
    "onCommand:everscript.openSettings",
    "onDebugResolve:everscript",
    "onDebugAdapterProtocol:everscript",
    "onCommand:everscript.buildAndRun"
  ],
  "categories": [
    "Programming Languages",
    "Snippets"
  ],
  "scripts": {
    "build:core:custom": "sh tools/build_snes_core.sh custom",
    "build:core:vanilla": "sh tools/build_snes_core.sh vanilla",
    "build:cores": "npm run build:core:vanilla && npm run build:core:custom",
    "test": "npm run build:cores && vscode-tmgrammar-test -g code_highlighter/syntaxes/everscript.tmLanguage.json 'code_highlighter/tests/**/*.test.evs' && node memory_radar/tests/activation.test.js && node memory_radar/tests/radar.test.js && node memory_radar/tests/damage.test.js && node memory_radar/tests/map-blob-evidence-model.test.js && node memory_radar/tests/render-script-model.test.js && node memory_radar/tests/smoke.test.js && node memory_radar/tests/ui.test.js && node memory_radar/tests/scaling-rom.test.js && node memory_radar/tests/map-pipeline-model.test.js && node debugger/tests/debugger.test.js && node debugger/tests/settings-model.test.js && node debugger/tests/snes-rom-header-model.test.js && node debugger/tests/room-script-model.test.js && node debugger/tests/parser-parity.test.js && node debugger/tests/emulator-health.test.js",
    "test:parity": "node debugger/tests/parser-parity.test.js",
    "test:parity:strict": "PARITY_STRICT=1 node debugger/tests/parser-parity.test.js",
    "test:emulator-runtime": "npm run build:cores && node debugger/tests/emulator-health.test.js",
    "package": "mkdir -p out && /opt/homebrew/bin/vsce package --out out/ 2>&1 || (echo '' && echo 'ERROR: vsce package failed. Check publisher/engine fields in package.json.' >&2 && exit 1)",
    "deploy": "npm run package && node -e \"const v=require('./package.json').version; const {execSync}=require('child_process'); const vsix='out/everscript-'+v+'.vsix'; console.log('Installing '+vsix+'...'); try { execSync('code --install-extension '+vsix+' --force', {stdio:'inherit'}); } catch(e) { console.error('\\nERROR: code CLI not found or install failed.'); console.error('Fix: add VS Code to PATH via Command Palette: Shell Command: Install code in PATH'); process.exit(1); } const out=execSync('code --list-extensions 2>&1').toString(); if(!out.includes('rbin.everscript')) { console.error('\\nERROR: extension installed but rbin.everscript not found in extension list.'); console.error('Installed output was: '+out); process.exit(1); } console.log('\\nOK: rbin.everscript@'+v+' is installed. Reload VS Code (Developer: Reload Window).')\"",
    "typecheck": "tsc --noEmit",
    "check:circular": "madge --circular --extensions js extension.js code_highlighter/ memory_radar/ debugger/",
    "check:dead": "knip"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "knip": "^6.14.2",
    "madge": "^8.0.0",
    "playwright": "^1.60.0",
    "typescript": "^6.0.3",
    "vscode-tmgrammar-test": "^0.1.3"
  },
  "contributes": {
    "languages": [
      {
        "id": "everscript",
        "aliases": [
          "Everscript",
          "evs"
        ],
        "extensions": [
          ".evs"
        ],
        "configuration": "./code_highlighter/language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "everscript",
        "scopeName": "source.evs",
        "path": "./code_highlighter/syntaxes/everscript.tmLanguage.json"
      }
    ],
    "themes": [
      {
        "label": "Everscript Dark",
        "uiTheme": "vs-dark",
        "path": "./code_highlighter/themes/everscript-dark.json"
      }
    ],
    "snippets": [
      {
        "language": "everscript",
        "path": "./code_highlighter/snippets/everscript.json"
      }
    ],
    "breakpoints": [
      {
        "language": "everscript"
      }
    ],
    "debuggers": [
      {
        "type": "everscript",
        "label": "Everscript Debug (Mock)",
        "program": "./debugger/adapter.js",
        "runtime": "node",
        "languages": [
          "everscript"
        ],
        "configurationAttributes": {
          "launch": {
            "required": [
              "program"
            ],
            "properties": {
              "program": {
                "type": "string",
                "description": "Path to the root .evs file to debug.",
                "default": "${file}"
              },
              "entryFunction": {
                "type": "string",
                "description": "Name of the function to start execution at.",
                "default": "trigger_enter"
              }
            }
          }
        },
        "initialConfigurations": [
          {
            "type": "everscript",
            "request": "launch",
            "name": "Debug .evs script",
            "program": "${file}",
            "entryFunction": "trigger_enter"
          }
        ],
        "configurationSnippets": [
          {
            "label": "Everscript: Debug script",
            "description": "Step through an .evs script in the mock runtime.",
            "body": {
              "type": "everscript",
              "request": "launch",
              "name": "Debug .evs script",
              "program": "^\"${file}\"",
              "entryFunction": "trigger_enter"
            }
          }
        ]
      }
    ],
    "commands": [
      {
        "command": "everscript.openEmulator",
        "title": "Open Emulator Panel",
        "category": "Everscript"
      },
      {
        "command": "everscript.openSettings",
        "title": "Open Everscript Settings",
        "category": "Everscript"
      },
      {
        "command": "everscript.buildAndRun",
        "title": "Build and Run in Emulator",
        "category": "Everscript"
      },
      {
        "command": "everscript.openMemoryRadar",
        "title": "Everscript: Open Memory Radar"
      }
    ],
    "keybindings": [
      {
        "command": "everscript.buildAndRun",
        "key": "f5",
        "when": "editorLangId == everscript"
      }
    ],
    "problemMatchers": [
      {
        "name": "everscript",
        "owner": "everscript",
        "fileLocation": [
          "relative",
          "${workspaceFolder}"
        ],
        "pattern": {
          "regexp": "^(.+):(\\d+):\\s+(error|warning|note):\\s+(.+)$",
          "file": 1,
          "line": 2,
          "severity": 3,
          "message": 4
        }
      }
    ],
    "taskDefinitions": [
      {
        "type": "everscript",
        "required": [
          "script"
        ],
        "properties": {
          "script": {
            "type": "string",
            "description": "Path to the root .evs file to compile"
          }
        }
      }
    ],
    "menus": {
      "editor/context": [
        {
          "when": "editorLangId == everscript",
          "command": "everscript.openMemoryRadar",
          "group": "navigation@30"
        }
      ]
    },
    "configuration": {
      "title": "Everscript",
      "properties": {
        "everscript.repoPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute path to the Everscript repository root. When set, the extension auto-fills `compilerPath`, `pythonPath`, `inDirectory`, `patchesDirectory`, and `romPath` if those values are still empty."
        },
        "everscript.compilerPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Override: absolute path to `everscript.py` or a compiled binary. Auto-filled from `repoPath` when empty."
        },
        "everscript.pythonPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Override: absolute path to the Python interpreter. Auto-filled from `<repoPath>/.venv/bin/python3` when empty."
        },
        "everscript.inDirectory": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to the `in/` directory containing `.evs` source files. Used for static analysis (scope detection, room tree, `#include` resolution). Auto-filled from `repoPath` when empty, otherwise defaults to `<workspace>/in`."
        },
        "everscript.patchesDirectory": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to the `patches/` directory. Used both for active-patch detection and as the default compiler `--patches` directory. Auto-filled from `repoPath` when empty, otherwise defaults to `<workspace>/patches`."
        },
        "everscript.romPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to the Secret of Evermore ROM (`.smc`, no header). Used by the Scaling tab and the Rooms tab ROM-backed vanilla/script readers. Auto-filled from `repoPath` when empty, otherwise defaults to `<workspace>/Secret of Evermore (U) [!].smc`."
        },
        "everscript.assetsPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to the assets directory. Should contain an `ingredients/` subfolder with `.webp` ingredient icons. Defaults to the bundled extension assets."
        },
        "everscript.snesCorePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Path to a custom `snes9x2005-wasm` Emscripten `.js` build (for example `debugger/core/snes9x2005-wasm/snes9x_2005.js`). The matching `.wasm` file must be in the same directory. Leave empty to use the bundled vanilla core at `debugger/core/snes9x2005-wasm-vanilla/snes9x_2005.js`.",
          "scope": "machine"
        },
        "everscript.patchesPath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Legacy alias for the compiler `--patches` directory.",
          "deprecationMessage": "Use Everscript: Patches Directory instead. This setting is kept only for backward compatibility."
        }
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rbin/everscript-vscode"
  },
  "license": "MIT"
}
